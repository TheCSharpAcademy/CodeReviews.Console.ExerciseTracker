<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <appSettings>
    <!-- Please replace the connection strings with your own SQL Server instance IP, database name and credentials -->
    <!-- Database must exist, although it can have no tables present -->
    <add key="ConnectionString" value="Server=192.168.1.57;Database=EXERCISE;TrustServerCertificate=True;User Id=exerciseuser;Password=!!exercisepassword;"/>
    <!-- Preferred DateTime format -->
    <add key="PreferredDateTimeFormat" value="dd-MM-yy HH:mm"/>
  </appSettings>
</configuration>